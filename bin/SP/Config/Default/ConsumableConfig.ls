config.consumable =
{
    pump =
    {
        cycle = 36,
        lastTime = _G.os.time(),
    },
    uvLamp =
    {
        cycle = 6,
        lastTime = _G.os.time(),
    },
    resin =
    {
        cycle = 12,
        lastTime = _G.os.time(),
    },
}
