config.remain =
{
    waste =
    {
        total = 2500,
        cycle = 6,
        lastTime = _G.os.time(),
    },
    reagent1 =
    {
        total = 2500,
        cycle = 6,
        lastTime = _G.os.time(),
    },
    reagent2 =
    {
        total = 2500,
        cycle = 1,
        lastTime = _G.os.time(),
    },
}

